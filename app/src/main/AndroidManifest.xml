<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nserdyuk.smartkid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bulb"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_bulb"
        android:supportsRtl="true"
        >
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeLarge"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".grades.GragePreschoolActivity"
            android:theme="@style/AppThemeLightLarge"
            />
    </application>

</manifest>